version: '3.7'

services:
    app:
        build:
            context: .
        volumes:
            - static_data:/vol/web
        environment: 
            - DJANGO_SECRET_KEY=secret
            - ALLOWED_HOSTS= localhost
    
    proxy:
        build:
            context: ./proxy
        volumes:
            - static_data:/vol/static
        ports:
            - "8080:8080"
        depends_on:
            - app

volumes:
    static_data:
